---
peer_review_model: qwen3.7-max
peer_review_date: 2026-08-01 16:45
spec_ref: layout-base-y-dashboard-spec.md
status: passed_with_findings
override_reason: ""
overridden_by: ""
decisiones: { aceptados: 5, descartados: 9, tareas: [T9.1, T9.2, T9.3, T9.4, T9.5] }
findings: { funcional: 0, tecnico: 17, criticos: 0, refutados: 7 }
lentes: { ok: 3, total: 3, degradado: false }
refutacion: ok
usage: { prompt: 300667, razonamiento: 52816, respuesta: 8115, total: 361598 }
---

# Peer Review: Layout Base y Dashboard

> Revisión independiente del modelo par **qwen3.7-max** (Fase 7.7): compara lo
> prometido (spec funcional aprobada + plan técnico) contra lo entregado (diff del
> Build). Cada hallazgo exige una **Decisión del equipo** (Aceptado → tarea T# ·
> Descartado: razón) — sin decisión, la fase está incompleta.

## Cobertura de la revisión

> Tres lentes con foco distinto, cada uno con su propio recorte del paquete. Un
> lente que falla no invalida a los otros, pero **sí** deja el informe incompleto.

| Lente | Foco | Estado | Hallazgos |
|-------|------|--------|-----------|
| Cobertura de lo prometido | `funcional` | ✅ ok | 7 |
| Robustez de lo entregado | `tecnico` | ✅ ok (diff en 2 partes) | 10 |
| Cumplimiento de la Constitución | `constitucion` | ✅ ok (diff en 2 partes) | 7 |

## Hallazgos funcionales
> Lo prometido en la spec (historias, escenarios, asunciones) que falta
> implementar o mejorar. Se validan contra `H#-E#` y `AF-#`.

_Sin hallazgos en esta categoría._

## Hallazgos técnicos
> Calidad, performance y robustez del código generado — lo que el plan diseñó
> pero la implementación no cubrió del todo (incluye artículos de la Constitución).

| ID | Hallazgo | Qué falta / mejorar | Motivo | Refs | Severidad | Decisión del equipo |
|----|----------|---------------------|--------|------|-----------|---------------------|
| PRT-1 | Sidebar.tsx envuelve SidebarSeccion en un <div onClick={onDesplegar}> para Comercial/Packing, lo que hace que TODO el enlace (incluyendo el <a> interno de react-router) dispare el handler de despliegue vía bubbling, pero además el <div> no tiene rol ni tabIndex, por lo que el click en el enlace navega Y despliega — sin embargo, si el usuario hace click derecho → 'Abrir en nueva pestaña' o usa teclado (Enter), el onClick del div NO se dispara consistentemente en todos los navegadores para teclado, y el grupo nunca se despliega vía teclado. | El wrapper <div onClick> debe reemplazarse por un handler en el propio SidebarSeccion (prop onActivar) o usar onKeyDown/onClick en el enlace directamente para garantizar que H1-E3/H1-E4 (despliegue al elegir) funcione también por teclado (H7-E15 exige Enter activa la sección). | H1-E3 y H1-E4 son P1: 'al elegir Comercial se despliega su grupo'. Si el usuario navega con Tab hasta 'Comercial' y presiona Enter (H7-E15), el enlace <a> se activa (navega a /comercial/ordenes) pero el onClick del <div> padre no necesariamente se dispara vía teclado en todos los navegadores — el evento click sintético de Enter en <a> no siempre burbujea como un click de mouse. Esto rompe el despliegue por teclado. | H1-E3, H1-E4, H7-E15, src/components/layout/Sidebar.tsx:128-134 | 🟡 Medio | ❌ Descartado: premisa falsa — Enter sobre un `<a>` dispara un click nativo real que sí burbujea (no es sintético); el propio T5.4 del plan ya lo documenta ("Es un NavLink, así que Enter lo activa sin código extra"). El toggle funciona por teclado. |
| PRT-2 | El store useTemaStore persiste en localStorage con clave 'layout.tema' usando el middleware persist de Zustand, pero no se configura versión ni migración. Si en una entrega futura la estructura del store cambia (ej. se agrega 'sistema' como opción), la hidratación fallará silenciosamente o cargará datos incompatibles. | Configurar version: 1 y una función migrate en el middleware persist de useTemaStore, o al menos documentar la clave como contrato. | A-3 declara useTemaStore persistido. RN-29 exige persistencia entre visitas. Sin version/migrate, un cambio futuro en la forma del estado (ej. agregar tema 'auto') causará hidratación con datos stale o crash silencioso de Zustand — el tipo de bug que aparece 6 meses después y es caro de diagnosticar. | A-3, RN-29, src/stores/useTemaStore.ts | 🟢 Bajo | ✅ Aceptado → T9.1 |
| PRT-3 | indicadores.api.ts simula latencia con setTimeout pero no implementa AbortController/CancelToken. Si el usuario navega fuera de InicioPage antes de que resuelva la promesa (ej. click rápido en sidebar durante loading), la promesa se resuelve igualmente y TanStack Query actualiza el cache de un componente ya desmontado. |  | H4-E1 exige estado de carga visible. Si el usuario navega rápido (Inicio → Almacén en <200ms), la promesa pendiente se resuelve sobre un componente desmontado. TanStack Query maneja esto gracefully en la mayoría de casos, pero sin cancelación explícita el setTimeout sigue corriendo y la función de resolución ejecuta código innecesario — riesgo de memory leak menor pero real si hay navegación rápida repetida. | H4-E1, A-2, src/features/dashboard/api/indicadores.api.ts | 🟢 Bajo | ❌ Descartado: riesgo teórico — TanStack Query ya ignora actualizaciones sobre componentes desmontados; es una API simulada sin costo real de red que justifique `AbortController`. |
| PRT-4 | SidebarMovil cierra el menú con un onClick en el <nav> completo que delega a todos los enlaces hijos, pero si el usuario hace click en el espacio vacío del <nav> (entre enlaces, o en el padding), el panel se cierra sin navegar — comportamiento inesperado. | Filtrar el onClick del <nav> para que solo cierre cuando el target es un <a> (enlace de navegación), no cuando se hace click en el contenedor vacío. | RN-32 dice 'SidebarMovil cierra al navegar'. Hacer click en el padding del <nav> no es navegar, pero el handler actual cierra el panel igualmente. El usuario abre el menú, toca accidentalmente un espacio vacío, y el panel se cierra sin haber elegido nada — fricción de UX en móvil. | RN-32, H7-E5, src/components/layout/SidebarMovil.tsx:55-57 | 🟡 Medio | ✅ Aceptado → T9.2 |
| PRT-5 | AppLayout.tsx usa h-dvh (100dvh) en la raíz, pero no hay fallback para navegadores que no soportan la unidad dvh (Safari <15.4, Chrome <108). En esos navegadores, h-dvh se ignora y el contenedor colapsa a altura auto. | Agregar un fallback min-height: 100vh antes de h-dvh, o usar la utilidad @supports de Tailwind. | H7-E1 y H7-E2 exigen cero scroll horizontal entre 360 y 1440px. Si h-dvh no se aplica en un navegador legacy, el contenedor raíz no tiene altura fija, el documento desarrolla scroll propio, y el overflow-hidden de la raíz no contiene nada — el defecto de scroll horizontal fantasma que T5.13 resolvió reaparece en navegadores antiguos. | H7-E1, H7-E2, src/components/layout/AppLayout.tsx:57 | 🟢 Bajo | ❌ Descartado: app corporativa interna (Agro Comercial), navegadores controlados/modernos; sin evidencia en la spec de soporte a navegadores legacy. |
| PRT-6 | useIndicador ignora cambios en los parámetros `modo` y `latenciaMs` después del montaje inicial | Incluir `modo` y `latenciaMs` en la `queryKey` o documentar explícitamente que estos parámetros son inmutables durante el ciclo de vida del componente | Si un componente padre re-renderiza con un `modo` diferente (ej: para forzar estado de error en demo), TanStack Query seguirá sirviendo datos cacheados del modo anterior porque la queryKey solo contiene `['indicador', id]`. Esto rompe el aislamiento estructural prometido en A-2 y hace que el parámetro `modo` sea efectivamente ignorado después del primer render, contradiciendo la documentación del hook que lo presenta como inyectable. | A-2, src/features/dashboard/hooks/useIndicador.ts:31 | 🟡 Medio | ✅ Aceptado → T9.3 |
| PRT-7 | TarjetaIndicador no maneja el caso donde `data` es undefined sin estar en estado de carga o error | Agregar un estado de fallback o validación defensiva para cuando `data` es undefined pero `isPending` e `isError` son ambos false | El componente asume que si no está cargando ni en error, entonces `data` existe y tiene `estado`. Sin embargo, TanStack Query puede tener `data: undefined` en edge cases (ej: después de `queryClient.clear()` o si el queryFn retorna undefined). Esto causaría un crash silencioso de React al intentar acceder a `data.estado` o `data.indicador`, violando la robustez del contrato de estados de AF-7. | AF-7, src/features/dashboard/components/TarjetaIndicador.tsx:115-140 | 🟡 Medio | ❌ Descartado: sobredimensionado — con optional chaining no hay crash, solo queda en blanco. Y el branch es inalcanzable hoy: `data` indefinido fuera de pending/error no ocurre en el ciclo de vida actual de TanStack sin `queryClient.clear()` manual. |
| PRT-8 | El store useTemaStore no valida el valor persistido en localStorage al hidratar | Agregar validación en el middleware `persist` para verificar que el valor recuperado sea 'claro' u 'oscuro' antes de aplicarlo | Si localStorage contiene un valor corrupto o inválido (ej: por manipulación manual o cambio de schema futuro), el store lo aceptará ciegamente y `useAplicarTema` aplicará una clase `dark` incorrecta o ninguna, rompiendo H6-E5/E6. Zustand persist no valida por defecto, y A-9 especifica que el tema debe ser siempre uno de los dos valores válidos. | A-9, H6-E5, src/stores/useTemaStore.ts:28-38 | 🟢 Bajo | ❌ Descartado: requiere manipular localStorage a mano para disparar. Bajo valor ahora. |
| PRT-9 | GrillaIndicadores no tiene estado de carga, error o vacío a nivel de grilla | Considerar si la grilla necesita un skeleton global o mensaje de error cuando todos los indicadores fallan simultáneamente | Actualmente cada tarjeta maneja su propio estado independientemente (H4-E11), lo cual es correcto. Sin embargo, si los 4 indicadores fallan al mismo tiempo, el usuario ve 4 tarjetas con error sin contexto global. El plan no especifica si esto requiere un mensaje adicional, pero la ausencia de manejo a nivel de grilla podría ser una experiencia subóptima en casos de falla total del backend futuro. | H4-E11, src/features/dashboard/components/GrillaIndicadores.tsx:23-31 | 🟢 Bajo | ❌ Descartado: el propio hallazgo admite que está fuera del alcance de la spec (H4-E11 exige aislamiento por tarjeta, no estado global). |
| PRT-10 | El tipo `IndicadorId` está hardcodeado a 4 valores específicos, limitando la reutilización del contrato | Evaluar si `IndicadorId` debería ser un string genérico o si la restricción a 4 valores es intencional y debe documentarse | AF-7 declara que `TarjetaIndicador` es el contrato reutilizable para todo el sistema futuro. Sin embargo, el tipo `IndicadorId` está restringido a exactamente 4 literales ('ordenes-mes', 'kilos-despachados', etc.), lo que impedirá usar el mismo componente para nuevos indicadores sin modificar el tipo. Esto contradice parcialmente la promesa de reutilización de RN-35. | AF-7, RN-35, src/features/dashboard/types/indicador.types.ts:15-19 | 🟢 Bajo | ❌ Descartado: alcance actual es 4 indicadores fijos por diseño; ampliar el type es trivial cuando llegue un 5to. |
| PRT-11 | Sidebar.tsx envuelve SidebarSeccion en un <div onClick> para desplegar subsecciones, pero el enlace interno sigue siendo foco y activable por teclado; el div no es interactivo ni tiene rol/aria, y el click handler no previene la navegación del Link anidado. | Usar un <button> separado para el toggle de despliegue (con aria-expanded) o manejar onKeyDown/role=button/tabIndex en el wrapper; evitar que el click del enlace dispare el toggle del padre. | A12 — responsabilidad clara y bajo acoplamiento en componentes interactivos; mezclar la activación del enlace con el toggle del grupo en un div no semántico rompe la separación de responsabilidades y la accesibilidad por teclado. | A12, src/components/layout/Sidebar.tsx:128-132 | 🟡 Medio | ❌ Descartado: mismo hallazgo que PRT-1 desde otro ángulo (A12) — se descarta con la misma razón: Enter sobre `<a>` burbujea nativamente, el toggle funciona por teclado. |
| PRT-12 | SidebarMovil.tsx cierra el menú con un onClick en el <nav> que envuelve todos los enlaces, sin distinguir si el click fue en un enlace o en otro elemento del nav. | Delegar el cierre solo cuando el target es un <a> (o usar onClick en cada enlace) para evitar cerrar el panel por clicks accidentales en el contenedor. | A12 — la lógica de cierre debe tener responsabilidad clara; cerrar el panel por cualquier click dentro del nav acopla comportamiento de UI a un contenedor y puede generar interacciones inesperadas. | A12, src/components/layout/SidebarMovil.tsx:58-60 | 🟢 Bajo | ✅ Aceptado → T9.2 (mismo hallazgo que PRT-4 desde otro ángulo — una sola tarea, no duplicada) |
| PRT-13 | El store useUiStore no persiste el estado de sidebarColapsado ni menuMovilAbierto, pero el README no documenta esta decisión ni las implicaciones de recarga. | Documentar en README o DESIGN.md que el estado de UI no persiste entre sesiones, para evitar confusión de usuarios/desarrolladores. | A10 — documentación en español y clara; la falta de documentación sobre persistencia puede llevar a malentendidos sobre el comportamiento esperado. | A10, README.md, src/stores/useUiStore.ts | 🟢 Bajo | ✅ Aceptado → T9.4 |
| PRT-14 | El componente EnConstruccion usa un ícono Wrench con aria-hidden='true' pero no provee texto alternativo ni contexto para lectores de pantalla sobre el estado de la sección. | Agregar un aria-label o texto visible que describa el estado 'en construcción' para usuarios de tecnología de asistencia. | A12 — accesibilidad y responsabilidad clara en componentes presentacionales; el ícono decorativo no comunica el estado a usuarios de lectores de pantalla. | A12, src/components/shared/EnConstruccion.tsx:20-22 | 🟢 Bajo | ❌ Descartado: falso positivo — verificado en el código: el ícono es decorativo (`aria-hidden` correcto) y el `<h2>`+`<p>` visibles ya comunican el estado a lectores de pantalla. |
| PRT-15 | El archivo `src/index.css` importa una fuente desde Google Fonts mediante una URL externa (`https://fonts.googleapis.com/...`) sin respaldo local ni estrategia de fallback explícita más allá de la declaración en `@theme inline`. | Si el entorno de producción o la red del usuario bloquea o falla al resolver `fonts.googleapis.com`, la aplicación podría quedar sin tipografía o con un FOUT (Flash of Unstyled Text) severo, afectando la experiencia visual aprobada en el DESIGN.md. | Aunque el código tiene un fallback (`sans-serif`), depender de un CDN externo para la tipografía principal en una aplicación corporativa (AgroMigiva) introduce un punto de fallo externo y problemas de privacidad/latencia que no fueron justificados en el plan técnico (A13). | src/index.css:5, A13 | 🟢 Bajo | ✅ Aceptado → T9.5 |
| PRT-16 | El hook `useIndicador` en `src/features/dashboard/hooks/useIndicador.ts` utiliza `queryKey: ['indicador', id]` pero no incluye los parámetros `modo` y `latenciaMs` en la clave. | Si un componente o prueba cambia el `modo` (por ejemplo, de 'con-valor' a 'falla' para simular un error) manteniendo el mismo `id`, TanStack Query devolverá el resultado cacheado de la llamada anterior en lugar de ejecutar la nueva función con el nuevo modo. | Esto rompe la capacidad de simular estados dinámicamente en tiempo de ejecución o en pruebas secuenciales sin limpiar la caché manualmente, violando la premisa de aislamiento y comportamiento predecible descrita en la documentación del hook (A2, A7). | src/features/dashboard/hooks/useIndicador.ts:33, A2, A7 | 🟡 Medio | ✅ Aceptado → T9.3 (mismo hallazgo que PRT-6 desde otro ángulo — una sola tarea, no duplicada) |
| PRT-17 | El componente `GrillaIndicadores` mapea `indicadoresMock` directamente para renderizar las tarjetas, pero el componente `TarjetaIndicador` obtiene sus datos de forma asíncrona mediante `useIndicador` (que a su vez llama a `obtenerIndicador`). | Existe una duplicación de la fuente de verdad para el `nombre` del indicador: la grilla lo pasa como prop desde el mock estático, mientras que el resto de los datos vienen de la API simulada. Si la API real devolviera un nombre diferente o actualizado, la UI mostraría una inconsistencia (el nombre de la prop vs el nombre que podría venir en `data`). | La arquitectura actual acopla la presentación de la grilla a un mock estático importado, en lugar de delegar la obtención de la lista completa de indicadores a una consulta de API (como `useIndicadores`), lo cual sería el patrón correcto para una lista dinámica (A12). | src/features/dashboard/components/GrillaIndicadores.tsx:23, A12 | 🟡 Medio | ❌ Descartado: comportamiento documentado y deliberado en el propio código (comentario de `TarjetaIndicador.tsx`: sin backend real aún, `GrillaIndicadores` pasa el mock a propósito). Se resuelve cuando exista API real, no ahora. |

## Descartados por el filtro adversarial
> Una segunda pasada del modelo par intentó **refutar** cada hallazgo. Estos no
> resistieron: quedan registrados para que la decisión de descartarlos sea
> auditable, no invisible.

| ID | Hallazgo descartado | Por qué no se sostiene |
|----|--------------------|------------------------|
| PRF-1 | El menú lateral de escritorio no se cierra al presionar Escape cuando está abierto | El hallazgo reconoce explícitamente que la interpretación más razonable es que RN-33 aplica solo a paneles superpuestos (notificaciones, usuario, menú móvil), no al sidebar de escritorio que es un panel fijo persistente. Además, H7-E18 especifica cierre con Escape únicamente para el menú deslizable en teléfono, no para el sidebar de escritorio. No hay violación de spec. |
| PRF-2 | El botón 'Abrir menú' del sidebar móvil no recibe foco con Tab como primer elemento del shell en teléfono | El propio hallazgo se retira tras análisis: verifica que en Topbar.tsx el botón 'Abrir menú' está antes del <h1> en el DOM (líneas 42-48), y que el <h1> no es enfocable (no tiene tabIndex). Por lo tanto, el primer Tab en teléfono SÍ va al botón 'Abrir menú'. Falso positivo confirmado por el propio revisor. |
| PRF-3 | La ruta de navegación no muestra 'Inicio' como primer nivel cuando la ruta no es reconocida | El hallazgo reconoce que el router (src/app/router.tsx) no define ruta catch-all y que la spec no exige manejo de rutas 404. AF-24 declara que no hay rutas protegidas ni validación de sesión. Es un edge case fuera del alcance funcional explícito de la spec. |
| PRF-4 | El aviso de 'En construcción' no se anuncia a lectores de pantalla como cambio de pantalla | El propio hallazgo se retira: H7-E21 exige que el cambio de pantalla se anuncie (el título), no el contenido completo. AnuncioPantalla.tsx cumple esto con aria-live='polite'. El aviso 'En construcción' es un <h2> estático que el lector de pantalla leerá cuando el usuario navegue al área de contenido. No hay violación de accesibilidad. |
| PRF-5 | El panel de notificaciones no tiene un rol 'dialog' o 'region' que lo identifique como panel independiente | El propio hallazgo se retira por estar fuera del alcance del reviewer funcional. La spec funcional no prescribe roles ARIA específicos para el panel de notificaciones. H5-E6 solo exige que se abra el panel. El uso de DropdownMenu de shadcn/Radix con rol 'menu' es una decisión de implementación válida. |
| PRF-6 | El esqueleto de carga de las tarjetas no tiene aria-busy ni rol de progreso | El propio hallazgo se retira: H4-E12 y H4-E13 exigen anuncio de mensajes de estado (error y vacío), pero no del estado de carga. RN-17 solo exige que el esqueleto sea visible y el nombre se conserve. En TarjetaIndicador.tsx líneas 98-100 el nombre está visible fuera del aria-live, por lo que el lector de pantalla lo leerá como contenido estático. |
| PRF-7 | El menú de usuario no se cierra al navegar a otra sección | El hallazgo reconoce que es teórico y probablemente no se manifiesta en la práctica. La spec no exige que el menú de usuario se cierre al navegar. H5-E13 solo especifica exclusión mutua entre paneles de la topbar. Radix DropdownMenu se cierra al hacer clic fuera (H5-E12) o con Escape (H7-E17), ambos implementados. Además, la navegación con Link mueve el foco al enlace, lo que cierra el menú por pérdida de foco. |

## Consumo
> Tokens de TODAS las pasadas (un lente por foco + la refutación). El razonamiento
> se cobra como salida y compite con el informe por el presupuesto de `max_output_tokens`.

| Concepto | Tokens |
|----------|--------|
| Entrada (paquete por lente) | 300,667 |
| Salida — razonamiento del modelo | 52,816 |
| Salida — informe devuelto | 8,115 |
| **Total** | **361,598** |
